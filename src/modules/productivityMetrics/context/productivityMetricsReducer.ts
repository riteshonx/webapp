import { Action } from '../../../models/context';
import { IProductivityMetricsReducer } from '../models/productivityMetrics';

export const productivityMetricsInitialState: IProductivityMetricsReducer = {}

export const productivityMetricsReducer = (
  state: IProductivityMetricsReducer,
  action: Action): IProductivityMetricsReducer => {
  return state
}
