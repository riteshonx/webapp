import React from 'react';
import { render, screen } from '@testing-library/react';
import ScheduleRecipes from '../ScheduleRecipes/ScheduleRecipes';

test('Renders Open Forms', () => {
  render(<ScheduleRecipes />);
});
